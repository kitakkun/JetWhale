<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- This permission is required to connect to the websocket server -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.kitakkun.jetwhale.demo.DemoApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="JetWhale Demo App">
        <activity
            android:name="com.kitakkun.jetwhale.demo.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Material.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep links for the Deep Links plugin: a custom scheme and an https App Link, both
                 routed to the demo's Navigation 3 back stack by DemoDeepLinks. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="demo" android:host="item" />
                <data android:host="settings" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="demo.jetwhale.example" android:pathPrefix="/item/" />
            </intent-filter>
        </activity>
    </application>
</manifest>
