# JetWhale Debugger Agent Runtime

This module provides the runtime environment for debugger agents built using the JetWhale Debugger
Agent SDK. It includes the necessary components to execute and manage debugger agents within the
JetWhale platform.

The debuggee application must include this runtime module to enable debugging capabilities.
